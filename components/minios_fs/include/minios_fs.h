#pragma once

/*
 * The public filesystem API is part of minios.h. This component header lets
 * clients declare an explicit dependency on the filesystem service without
 * exposing LittleFS or ESP-IDF types.
 */
#include "minios.h"
