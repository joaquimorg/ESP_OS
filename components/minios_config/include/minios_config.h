#pragma once

/*
 * The public configuration API is part of minios.h.  This component header
 * exists so components can explicitly declare their dependency on the
 * persistent configuration service without exposing ESP-IDF NVS types.
 */
#include "minios.h"
